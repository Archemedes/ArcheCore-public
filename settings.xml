<settings>
    <servers>
        <server>
            <id>lotc-release</id>
            <username>${env.USER_TOKEN}</username>
            <password>${env.PWD_TOKEN}</password>
        </server>
    </servers>
</settings>